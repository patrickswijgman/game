export const enum Texture {
  ATLAS,
}

export const enum Font {
  DEFAULT,
}

export const enum Type {
  UNKNOWN,
  PLAYER,
}

export const enum Input {
  UP = "KeyW",
  DOWN = "KeyS",
  LEFT = "KeyA",
  RIGHT = "KeyD",
}
