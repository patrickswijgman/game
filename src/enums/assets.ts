export const enum TextureId {
  NONE,
  ATLAS,
}

export const enum SpriteId {
  NONE,
  PLAYER,
  PLAYER_SHADOW,
  TREE_PINE,
  TREE_SHADOW,
  TILE_GRASS,
  CARD,
  CARD_SHADOW,
  LONGSWORD,
}

export const enum FontId {
  DEFAULT,
}
