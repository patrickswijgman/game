export const enum TextureId {
  NONE,
  ATLAS,
  ATLAS_OUTLINE,
  MAP_TILES,
  MAP_OBJECTS,
  FLOOR,
}

export const enum SpriteId {
  NONE,
  PLAYER,
  PLAYER_SHADOW,
  TREE_PINE,
  TREE_SHADOW,
  TILE_GRASS,
  CARD,
  CARD_SHADOW,
  CARD_OUTLINE,
  LONGSWORD,
}

export const enum FontId {
  DEFAULT,
}
